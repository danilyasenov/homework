import React from "react";
import "../styles.css"; // Убедись, что стили загружены

const Spinner = () => {
  return (
    <div className="spinner">
      <div></div>
    </div>
  );
};

export default Spinner;
